{
  "watch": ["src", ".env"],
  "ext": "ts,js,json",
  "ignore": ["node_modules", "dist", "coverage"],
  "exec": "ts-node-dev --respawn --clear --transpile-only src/app.ts",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
